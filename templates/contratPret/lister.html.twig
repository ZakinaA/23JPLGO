<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

LISTE DES CONTRATS DE PRET</br>
<table>
    {% for e in pContratsPret %}
        <tr>
        <td><a href="{{ path('app_contratPretConsulter', { 'id': e.id }) }}">Emprunt {{ e.id }}</a></td>
        <td>effectué par {{ e.eleveId.prenom ~ " " ~ e.eleveId.nom }} dont les responsables sont {% for eleve in pEleves %}
        <td>je suis un test {{ e.eleveId. }}
            {% for responsable in eleve.responsable %}
                {{ responsable.prenom }} et
            {% else %}
                Pas de responsables
            {% endfor %}
        </td>
    {% else %}
        Pas d'élèves
    {% endfor %}
        </a></td>
        <td>pour l'instrument {{ e.instrument.id }}</td>
    {% else %}
        </tr>
        <tr>
            <td>Aucun contrat de prêt n'a été trouvé.</td>
        </tr>
    {% endfor %}
</table>


</body>
</html>