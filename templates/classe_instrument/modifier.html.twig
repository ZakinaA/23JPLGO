{% extends 'base.html.twig' %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block header %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-content">
        <h5>MODIFICATION D'UNE CLASSE D'INSTRUMENT</h5>

        <section class="pt-0">
            <div class="container-fluid">
                <div class="row gy-4">
                    <div class="col-lg-12">
                        <div class="card">
                            <div class="card-header">
                                <h3 class="h4 mb-0">Modifier une classe d'instrument</h3>
                            </div>
                            <div class="card-body pt-0">
                                <form method="post" action="{{ path('classeInstrumentModifier', {'id': classeInstrument.id}) }}">
                                    {{ form_start(form) }}

                                    <div class="mb-3">
                                        <label class="form-label" for="exampleInputEmail1">Libellé de la classe d'instrument</label>
                                        {{ form_widget(form.libelle) }}
                                    </div>

                                    <!-- Ajoutez d'autres champs de formulaire au besoin -->



                                    {{ form_end(form) }}
                                </form>

                                <!-- Ajoutez un lien pour revenir à la consultation de la classe d'instrument -->
                                <a href="{{ path('classeInstrumentConsulter', {'id': classeInstrument.id}) }}">Retour à la consultation de la classe d'instrument</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}

{% block footer %}
    {{ parent() }}
{% endblock %}
