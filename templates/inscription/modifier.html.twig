{% extends 'base.html.twig' %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block header %}
    {{ parent() }}
{% endblock %}
{% block body %}

<br>
<section class="pt-0">
    <div class="container-fluid">
        <div class="row gy-4">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="h4 mb-0">Modifier une inscription</h3>
                    </div>
                    <div class="card-body pt-0">
                        <form action="{{ path('inscriptionModifier', { 'id': inscription.id }) }}" method="post">
                        {{ form_start(form) }}
                            <div class="mb-3">
                                <label for="date_inscription">Date de l'Inscription :</label>
                                {{ form_widget(form.date_inscription) }}
                            </div>
                            <div class="mb-3">
                                <label for="eleve">Elève :</label>
                                {{ form_widget(form.eleve_id) }}
                            </div>
                            <div class="mb-3">
                                <label for="cours">Cours :</label>
                                {{ form_widget(form.cours_id) }}
                            </div>
                            <div class="mb-3">
                                {{ form_end(form) }}
                        </form>

{% endblock %}